// User
enum Role {
  driver
  passenger
  admin
}

enum Status {
  active
  inactive
  banned
}

enum Gender {
  male
  female
  other
}

// Ride
enum RideStatus {
  pending
  accepted
  in_progress
  completed
  cancelled
}

// Payment
enum PaymentType {
  cash
  gcash
}
